{% if ourProjectsSubTitle or ourProjectsTitle or ourProjectList %}
  <div class="projects-main-sec overflow-hidden">
    <div class="container">
      <div class="row">
        <div class="col-full">
          <div class="heading-main-sec text-start">
            {% if ourProjectsSubTitle %}
              <h3><span>{{ ourProjectsSubTitle }}</span></h3>
            {% endif %}
            {% if ourProjectsTitle %}
              <h4 class="font-100">{{ ourProjectsTitle }}</h4>
            {% endif %}
          </div>
        </div>
        <div class="col-full projects-list">
          {% if ourProjectList and ourProjectList | length > 0 %}
            {% for item in ourProjectList %}
              <div class="projects-item">
                {% if item.projectImg %}
                  <img src="{{ item.projectImg }}" alt="{{ item.alt }}">
                {% endif %}
              </div>
            {% endfor %}
          {% endif %}
        </div>
      </div>
    </div>
  </div>
{% endif %}
