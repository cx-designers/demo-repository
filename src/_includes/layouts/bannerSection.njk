<section class="banner-section">  
<div class="d-flex flex-wrap align-items-center">
<div class="left-box col-6 px-4">
  <h1 class="mb-4">{{title}}</h1>
  <p>{{BannerDesc}}</p>
</div>
<div class="Right-box col-6 px-4">
  <img src="{{bannerImage.src}}" alt="{{bannerImage.alt}}" class="w-100" />
</div>
</div>
<div class="cursor"></div>
</section>